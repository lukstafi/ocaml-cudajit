# Proposal: task-3124de24 — Fix broken test suite: ocaml-cudajit

## Goal

Verify that ocaml-cudajit tests pass on the NVIDIA GPU worker (`minipc-wsl`), and confirm whether the test failure is an environment false positive (running on wrong machine) or a genuine regression.

## Context

The ocaml-cudajit test suite is failing on Mac Studio (Apple Silicon, no CUDA hardware). The build itself fails when trying to compile CUDA FFI stubs because `cuda.h` and `nvrtc.h` headers are not present — CUDA is structurally unavailable on ARM64 macOS.

The project was last tested successfully on 2025-08-19 across Ubuntu 22.04 and Windows CI pipelines. The comprehensive test suite (7 GPU tests + 1 NVRTC-only test) was added in July 2025 and passed all CI runs. No source changes have been made since then.

The intended environment for CUDA testing is `minipc-wsl` (Linux, NVIDIA GPU, CUDA toolkit installed). This machine was offline at elaboration time, so direct verification could not be completed.

## Acceptance Criteria

- [ ] Establish SSH connection to `minipc-wsl` or confirm it is online
- [ ] Run `dune runtest` on `minipc-wsl` and verify all tests pass
- [ ] If tests pass: close task as environment false positive, update health check config to require GPU
- [ ] If tests fail: diagnose root cause (CUDA version mismatch, OCaml environment drift, or genuine regression) and fix

## Approach

1. SSH into `minipc-wsl` and verify CUDA environment
2. Clone/update ocaml-cudajit to latest commit
3. Run `dune runtest` and capture output
4. If passing: update health check configuration to only run ocaml-cudajit tests on GPU-equipped machines
5. If failing: investigate specific failure (stubs compilation, test output mismatch, or runtime error) and fix

## Notes

- Task is blocked on `minipc-wsl` being online and accessible via SSH
- If minipc-wsl remains unavailable, consider scheduling this as a maintenance task for later or delegating to a machine with CUDA access
- CI was fully green on 2025-08-19; no regression expected if environment is correct
- `CUDAJIT_NO_GPU=true dune test test_no_device` should pass on any machine (NVRTC-only, no GPU required)
